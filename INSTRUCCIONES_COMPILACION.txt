# ═══════════════════════════════════════════════════════════════════
# INSTRUCCIONES PARA COMPILAR GEOWIZARD A EJECUTABLE (.EXE)
# ═══════════════════════════════════════════════════════════════════

## 📋 REQUISITOS PREVIOS

1. Python 3.8 o superior instalado
2. Todas las dependencias del proyecto instaladas (ver requirements.txt)
3. PyInstaller instalado

---

## 🚀 PASO 1: INSTALAR PYINSTALLER

Abrir terminal en la carpeta del proyecto y ejecutar:

```bash
pip install pyinstaller
```

---

## 🔧 PASO 2: EJECUTAR EL SCRIPT DE COMPILACIÓN

Opción A - Usando el script automático (RECOMENDADO):
```bash
build_exe.bat
```

Opción B - Comando manual:
```bash
pyinstaller geowizard.spec
```

---

## 📂 PASO 3: UBICACIÓN DEL EJECUTABLE

Después de la compilación exitosa:

- Carpeta temporal: `build/GeoWizard/`
- **Ejecutable final**: `dist/GeoWizard/GeoWizard.exe` ← ESTE ES EL QUE NECESITAS

---

## 📦 PASO 4: DISTRIBUCIÓN

### Opción 1: Carpeta Completa (RECOMENDADO)
- Entregar toda la carpeta `dist/GeoWizard/` como un ZIP
- Los usuarios solo descomprimen y ejecutan GeoWizard.exe
- Tamaño aproximado: ~150-200 MB

### Opción 2: Ejecutable Único (Más lento al iniciar)
Si prefieres un solo archivo .exe (sin carpetas):
```bash
pyinstaller --onefile geowizard.spec
```
⚠️ Advertencia: Será más lento al arrancar (descomprime en temp cada vez)

---

## ✅ VERIFICACIÓN

1. Navegar a `dist/GeoWizard/`
2. Hacer doble clic en `GeoWizard.exe`
3. Verificar que:
   - ✓ El logo de Tellus aparece en la ventana
   - ✓ Los iconos SVG se cargan correctamente
   - ✓ El mapa OSM funciona
   - ✓ Se pueden importar/exportar archivos
   - ✓ Al cerrar aparece el mensaje de agradecimiento

---

## 🐛 SOLUCIÓN DE PROBLEMAS

### Error: "No se encuentra el módulo X"
```bash
pip install X
pyinstaller --clean geowizard.spec
```

### Error: "No se encuentra tellus_logo.png"
- Verificar que el archivo spec incluye la carpeta icons/ en datas
- Recompilar con `pyinstaller --clean geowizard.spec`

### Error: "El mapa no carga"
- Verificar que map_base.html y carpeta leaflet/ estén en datas
- Verificar conexión a internet (requiere tiles de OSM)

### El ejecutable es muy grande
- Normal: PySide6 y QtWebEngine ocupan ~150 MB
- Incluye motor de navegador completo (Chromium)

---

## 📝 NOTAS IMPORTANTES

1. **NO subir a GitHub**: El ejecutable y carpetas build/dist/ están en .gitignore
2. **Primera ejecución lenta**: Windows Defender puede escanear el .exe
3. **Antivirus**: Algunos antivirus marcan ejecutables de PyInstaller como sospechosos (falso positivo)
4. **Distribución**: Comprimir `dist/GeoWizard/` en ZIP para facilitar la distribución

---

## 📊 ESTRUCTURA FINAL

```
dist/
└── GeoWizard/
    ├── GeoWizard.exe          ← EJECUTABLE PRINCIPAL
    ├── icons/
    │   ├── tellus_logo.png
    │   ├── *.svg
    │   └── ...
    ├── leaflet/
    │   └── ...
    ├── map_base.html
    ├── _internal/              ← Librerías (Python, Qt, etc)
    │   ├── PySide6/
    │   ├── pyproj/
    │   └── ...
    └── ... (otros archivos DLL y dependencias)
```

---

## 🎯 RESUMEN DE COMANDOS

```bash
# 1. Instalar PyInstaller (solo una vez)
pip install pyinstaller

# 2. Compilar (elige uno)
build_exe.bat              # Opción recomendada
pyinstaller geowizard.spec # Opción manual

# 3. Probar
cd dist\GeoWizard
GeoWizard.exe

# 4. Distribuir
# Comprimir toda la carpeta dist\GeoWizard\ en un ZIP
```

---

## 📞 SOPORTE

Si encuentras problemas durante la compilación:
- Revisar los logs en build/GeoWizard/warnings.txt
- Verificar que todas las dependencias estén instaladas
- Intentar `pyinstaller --clean geowizard.spec`

---

**¡Listo! Tu ejecutable de GeoWizard estará en `dist/GeoWizard/GeoWizard.exe`** 🎉
